# JobScheduler

Bootstrap for the Wingie / Enuygun job scheduling case.

This repository starts with a .NET 8 solution containing:

- `JobScheduler`: class library for the scheduling logic
- `JobScheduler.Tests`: xUnit test project

The actual DAG scheduling implementation, test scenarios, and case notes will be added in follow-up commits.
